<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Ночной вариант темы приложения — см. пояснение в values/styles.xml. Здесь только AppTheme:
         SettingsTheme берётся из values/, она и так DayNight и в ночном варианте не нуждается. -->
    <style name="AppTheme" parent="@android:style/Theme.DeviceDefault">
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>
