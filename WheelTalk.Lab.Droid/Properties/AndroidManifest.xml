<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
	<!-- Разрешений нет ни одного, и это свойство стенда: он не разговаривает с колесом и не сигналит
	     — только проигрывает то, что лежит в пакете. Своя база у него с 04.08.2026 (LabStore, история
	     для графиков), но лежит она в собственном каталоге приложения и разрешений не требует. -->
	<application android:allowBackup="false" android:supportsRtl="true" android:icon="@mipmap/appicon" android:theme="@style/LabTheme" />
</manifest>
