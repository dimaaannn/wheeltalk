# Нарезка для отладки

Куски настоящих поездок, вырезанные из сырых дампов 28.07.2026 — по одному характерному
состоянию в каждом. Приложение проигрывает их вместо колеса: настройка `Replay:DumpFile`,
рецепт целиком — в [AGENTS.md](../AGENTS.md), раздел «Как гонять приложение без колеса».

Формат тот же, что пишет приложение: `HH:mm:ss.fff,<hex>`, одна строка на BLE-нотификацию.
Файл проигрывается по кругу, так что короткого куска хватает надолго.

Откуда взяты и что в них происходит — [первый выход](../logs/field-2026-07-28/report.md)
(Sherman L) и [второй](../logs/field-2026-07-28-mten3/report.md) (MTen3).

| Файл | Колесо | Длит. | Что там |
|---|---|---|---|
| `mten3-parking.csv` | MTen3 | 75 с | Колесо стоит: скорость 0, ШИМ 2 %, напряжение ровное 80,4 В. Для проверки экрана в покое |
| `mten3-calm-ride.csv` | MTen3 | 2 мин | Спокойная езда 10–25 км/ч, ШИМ до 59 %, просадка 79,6 → 77,4 В. Основной кусок для работы над интерфейсом |
| `mten3-alarm-spinup.csv` | MTen3 | 12 с | Раскрут в воздухе до 47 км/ч, **ШИМ 110 %** — тревога срабатывает в полный голос. Для проверки тревожной рамки, звука и всего, что реагирует на предел |
| `mten3-slowing-to-stop.csv` | MTen3 | 50 с | Замедление с 18 км/ч до полной остановки |
| `shermanl-parking.csv` | Sherman L | 18 с | Стоящее колесо, 147 В. Короткий: в записях Sherman L стоял мало |
| `shermanl-calm-ride.csv` | Sherman L | 2 мин | Спокойная езда 6–32 км/ч, ШИМ до 43 %. Записано с погашенным экраном |
| `shermanl-spinup-cutout.csv` | Sherman L | 10 с | Раскрут до **145 км/ч**, ШИМ упирается в 100 %, колесо отключается. Крайний случай: и тревога, и обрыв связи следом |

Два колеса не взаимозаменяемы: у Sherman L протокол Veteran и пак ~150 В, у MTen3 — Gotway и
~80 В, и аппаратного ШИМ у него нет. Проверять интерфейс стоит на обоих: числа отличаются в разы,
и вёрстка, которая держит 145 км/ч, не обязана держать трёхзначный ШИМ.

Дампы — не тесты. Тестовые фикстуры лежат в `WheelTalk.Tests/Fixtures/`, и трогать их отсюда
не нужно.

## InMotion V1/V2 — настройка протокола реплея

У InMotion V1 и V2 кадры одинаковые (`AA AA`), по заголовку их не развести — а у записанной
поездки нет дерева GATT, по которому это делает живое подключение (план 21 §0.2). Для дампа
InMotion протокол называется настройкой `Replay:Protocol` (`"InMotion"` / `"InMotionV2"`) —
без неё `AutoDecoder` попытается опознать кадры по заголовку и не узнает `AA AA` ни за один, ни за
другой, и дамп останется без телеметрии. Для всех остальных протоколов (Veteran/Begode/KingSong)
настройка не нужна и должна оставаться пустой — они называют себя сами по заголовку кадра.

Своих нарезок для InMotion в этой папке пока нет — живого InMotion у владельца нет, и нарезать
характерные состояния не с чего. Единственные записи — `WheelTalk.Tests/Fixtures/RAW_inmotion_*.csv`
(из оригинала WheelLog, `Wheellog.Android/app/src/test/resources/`): полные сырые дампы, не
нарезка по состояниям, и туда же, а не сюда, они и должны попадать при появлении реальной
записи с колеса.
