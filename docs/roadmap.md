# Порядок работ

Планов двадцать два, и порядок их номеров — это порядок, в котором они писались, а не в котором их
делать. Здесь — второе. Файл короткий нарочно: подробности в самих планах, тут только
последовательность и причины именно такой. Вне нумерованной серии живёт план плеера —
[playback-plan.md](playback-plan.md), со своей внутренней очередью.

Правило, по которому всё выстроено: **сначала то, что дёшево и снимает риск с последующего**.
Выход к колесу стоит одного вечера и обесценивает половину догадок; движок настроек стоит недели
и не проверяет ничего.

> **Разборы уже закрытых работ** — заливка 28.07, снятие журналов с телефона, опознание колеса,
> «пункт 9» про отброшенный результат записи, переподключение в ядре, «тревога гасла от пустого
> окна» — вынесены в [archive/roadmap-razbory.md](archive/roadmap-razbory.md) при чистке
> документации 02.08.2026. Выводы всех шести вошли в код и AGENTS.md; текст сохранён дословно.

---

## Где мы сейчас (02.08.2026)

Приложение — нативный `com.wheeltalk.droid`; MAUI-версия и замерялка кадров удалены после приёмки
([план 12](android-plan-12-native-rewrite.md) и [план 19](android-plan-19-ui-seams.md) А1). Пять
протоколов, поездки в SQLite, плеер записей, стенд панели. Состояние железок и что проверено
глазами — [handover.md](handover.md).

**Ближайшее:**

1. **Выехать к колесу.** Очередь упирается в это: накопились изменения, собранные и проверенные на
   реплее, но не видевшие колеса. Список гейтов — [handover](handover.md), «Первое, за что браться».
2. **[План 22](android-plan-22-top-inset-and-replay-control.md)** — инсет полосы тревоги под
   системной строкой и управление реплеем без касаний. §2 удешевляет каждый следующий агентский
   прогон, поэтому его стоит делать до больших заходов.
3. Хвосты панели: из [dashboard-feedback.md](dashboard-feedback.md) «Прогона 4» не сделаны пункты
   3 и 5; полоса тревоги всё ещё член разметки, а не накладка (handover, «Дальше»).

---

## Очередь

**Сделано и закрыто** — планы [1](android-plan-1-base-app.md), [2](android-plan-2-ui-analysis.md),
[3](android-plan-3-main-screen.md), [4](android-plan-4-alerts-and-background.md),
[5](android-plan-5-ride-logging.md), [6](android-plan-6-settings-and-chrome.md),
[7](android-plan-7-dashboard-design.md) (кроме фазы 3), [8](android-plan-8-ride-database.md),
[12](android-plan-12-native-rewrite.md), [13](android-plan-13-debts.md),
[14](android-plan-14-refactoring.md), [19](android-plan-19-ui-seams.md),
[20](android-plan-20-proxy-import.md), [21](android-plan-21-kingsong-inmotion.md). Что именно и
чем проверено — в шапке каждого.

| # | Что | Зависит от | Почему не раньше |
|---|---|---|---|
| 1 | [План 9](android-plan-9-prediction.md), фаза 1 — **накопление данных** | — | Идёт фоном и не блокирует ничего: запись включена по умолчанию, данные копятся сами. Кода тут нет — это выезды. Мешает одно, см. ниже |
| 2 | [План 11](android-plan-11-field-robustness.md) — надёжность в поле | — | **В работе, двенадцать пунктов сделано** (сверено с кодом 01.08.2026). В P0 остался один — §3.2, причина отказа связи: подписки на `ActionStateChanged` нет |
| 3 | [План 22](android-plan-22-top-inset-and-replay-control.md) — инсет уведомлений и реплей без касаний | — | Записан со слов владельца 02.08.2026 по скриншоту. §2 окупается на первом же агентском прогоне |
| 4 | [План 16](android-plan-16-screen-on-return.md) — **экран, который встречает райдера** | — | Требование владельца 30.07.2026: включил дисплей — видишь панель, а не рабочий стол. Ждёт ответа, как телефон убирается в карман (§6 плана) |
| 5 | [План 9](android-plan-9-prediction.md), раскрутка в воздухе + фазы 2–4 | данным из п. 1 | Раньше нечего считать. Сколько ждать — покажут данные, а не календарь |
| 6 | [План 7](android-plan-7-dashboard-design.md), фаза 3 | — | Проверка идей владельца поштучно, на живых данных |
| 7 | [Плеер](playback-plan.md), §3–§5 | — | §0 сделан 30.07.2026. Осталось: длинный тап по строке списка, мелочи второй очереди |
| 8 | [План 17](android-plan-17-main-screen-variants.md) — **несколько главных экранов на выбор** | — | Написан 01.08.2026. Рамки: шторку не трогаем, общая логика не меняется, меняется только отображение. Первым делом — контракт главного экрана |
| 9 | [План 18](android-plan-18-diagnostics-kit.md) — **диагностика: собирать больше и прибирать за собой** | — | Написан и отложен владельцем 01.08.2026. **Поглощает §4.2–4.5 плана 11**, отдельно их не делать |
| 10 | [План 10](android-plan-10-telemetry-out.md) — телеметрия наружу | — | Не начат, место не назначено: у него свой внутренний порядок, ни от чего не зависит |

**Названо владельцем 01.08.2026, планов под это ещё нет** — каждому первым делом нужен план, а не
правка:

| Что | Почему и с чего начать |
|---|---|
| **Состав справочных значений — выбором в настройках** | Нижняя часть центра панели (макс. ШИМ, температура, поездка, заряд) должна набираться человеком. **Решение уже написано** — [dashboard-feedback.md](dashboard-feedback.md), прогон 5 §4, четырьмя шагами: выделить виджет из `SpeedBlockDrawable`, завести каталог полей, показать тот же виджет в плеере плотнее, и только потом тумблер на поле. Чего нельзя нарушить: состав не меняется от значений телеметрии, полуторный кегль по ISO 15008 сохраняется |
| **Убрать личные данные из приложения — перепроверить** | Адрес Sherman L из заводских настроек убран (`f19a726`), но остался в консольном `WheelTalk.Test.ConsoleConnection/appsettings.json`, а по репозиторию рассыпаны MAC-адреса в тестах, планах и разборах выездов. Пройти поиском по всему решению и решить по каждому месту: убрать, обезличить или оставить осознанно |
| **Мелочи, найденные и не исправленные** | Список — в [handover](handover.md). **Скачок пробега стоит взять раньше прочего:** запись включена по умолчанию, и каждая новая поездка уносит дефект в архив, на котором стоит план 9 |
| ~~**Не писать в базу на малой скорости**~~ (владелец, 02.08.2026) | **Сделано в тот же день** вариантом «как в оригинале»: порог на старт записи `Logging:AutoStartAboveKmh` = 7 км/ч. Разбор, почему не фильтр по каждой строке, — во врезке ниже |

---

## Что идёт параллельно, а не в очереди

- **План 9, фаза 1** — накопление данных: запись включена по умолчанию, дальше копится само.
- **Мелкие возвраты к логике оригинала** — см. «Отклонения от оригинала» в [AGENTS.md](../AGENTS.md).
  Это не этап, а то, что делается по ходу и проверяется перед каждым выходом на колесо.

## Чего в очереди нет намеренно

Второй язык и переключение на лету, GPS-блок в CSV, очистка старых данных, тревоги по току и
температурам, экспорт из интерфейса. Всё это лежит в разделах «Дальнейший план» своих планов и
ждёт причины, а не очереди.

---

## Порог скорости при записи — что на самом деле в оригинале

> **Названо владельцем 02.08.2026:** не писать данные в базу при скорости меньше 5 км/ч, «вроде
> такая же логика и в оригинальном проекте». Сверка в тот же день показала, что логика там
> другая (ниже), и владелец выбрал **делать как в оригинале**.
>
> ## ✅ СДЕЛАНО 02.08.2026
> `Logging:AutoStartAboveKmh`, по умолчанию **7 км/ч** — как `startAutoLoggingWhenIsMovingMore`
> оригинала. Автозапись ждёт первого отсчёта быстрее порога, а не подключения
> (`CrashGuard.SubscribeAppLevelHandlers`); ноль возвращает прежнее поведение «писать с
> подключения». Взятый однажды порог больше не проверяется: `RideRecorder.Start()` идемпотентен,
> и каждый следующий отсчёт попадает в ту же запись — стоянки внутри поездки пишутся.
> **Управляется с экрана «Запись»** — там же, где соседние `AutoStartRide` и `RawDump`:
> переключатель «Ждать начала движения» переводит порог между нулём и выбранным числом (само
> число правится файлом настроек, тумблер его помнит), а подпись под «Включать запись
> автоматически» стала честной — «когда колесо поедет быстрее 7 км/ч» вместо прежнего «при
> подключении к колесу», которое после этой правки врало бы. Проверено сборкой; **на живом
> колесе не проверено** — это гейт ближайшего выезда: доехать до 7 км/ч и убедиться, что запись
> началась сама.
>
> Попутно закрыт дефект, который эта же правка и создала бы: `UserSettingsStore.SaveLogging`
> переписывает раздел `Logging` целиком, и новый порог затирался бы при каждом переключении
> соседнего тумблера. Теперь в записи все три поля, и об этом сказано комментарием — четвёртое
> поле обязано попасть туда же.

Что в оригинале есть на самом деле (`MainActivity.kt:439-444`, `AppConfig.kt:254`):

- **Порог только на старт записи, а не на каждую строку.** `startAutoLoggingWhenIsMovingMore`
  (по умолчанию **7 км/ч**, не 5) отвечает на вопрос «когда начать писать»: как только скорость
  впервые превысила порог, запускается `LoggingService`. Ноль — писать с момента подключения.
- **Начавшись, запись идёт непрерывно**, стоянки и светофоры включительно. Фильтра «эту строку не
  писать, потому что колесо стоит» в оригинале нет вовсе.
- **`RIDING_SPEED = 200` (2 км/ч)** к записи отношения не имеет — по нему считается только «время
  в движении» (`WheelData.java:40,170`).

**Почему это не мелкая разница.** Фильтр на каждую строку убьёт данные, на которых стоит
[план 9](android-plan-9-prediction.md): кривая «напряжение покоя ↔ заряд» строится **именно на
стоянке и на зарядке** — там скорость нулевая по определению (см. следующий раздел). Оригинал
поэтому и разбирается со стоянками при чтении, а не при записи.

**Выбран вариант «как в оригинале»** (владелец, 02.08.2026): порог на старт записи, настройкой,
дефолт 7 км/ч. Стоянка внутри поездки пишется, данные для плана 9 целы, правило «считаем как
WheelLog» соблюдено.

Два отвергнутых варианта — чтобы к ним не возвращались по кругу:

- **Фильтровать каждую строку по скорости** (как прозвучало изначально) — отклонение от
  оригинала, и оно **закрывает дорогу прогнозам запаса хода**: кривая покоя строится на стоящем
  колесе.
- **Прореживать записи на малой скорости** (раз в 10 секунд вместо пяти раз в секунду) — держит
  базу худой и кривую покоя сохраняет; не понадобилось, но вернуться к нему стоит, если база
  когда-нибудь начнёт мешать. Для масштаба: полчаса у зарядки — около 9 тысяч строк, при
  прореживании тридцать.

---

## Что мешает фазе 1 плана 9 идти совсем само

Одна вещь. План 9 §3 требует **записи на стоянке и во время зарядки**: кривая «напряжение покоя ↔
заряд» строится именно тогда, когда колесо стоит, и без неё не будет ни остатка хода, ни кривой
опасности.

**Развилка оказалась ложной — в оригинале смотрели, и он не решает это при записи вовсе.**
`LoggingService` пишет всё подряд, пока подключён, стоянку включительно; со стоянкой разбираются
**при чтении** (`data/TripParser.kt:135-166`), тремя приёмами:

- **Перерывы вычитаются из длительности** — промежуток больше секунды считается простоем и не
  входит в `logDuration`, по которому считается расход. Иначе ночь на зарядке превратила бы
  Вт·ч/км в ноль.
- **Одометр берётся с первого ненулевого** (`firstTotalDistance`) — ровно наш известный дефект
  ложного скачка на разностях. У них он обойдён, у нас записан в «мелочи».
- **Конец пробега — максимум по последним десяти строкам**, а не последняя строка: одно битое
  значение в хвосте иначе задаёт весь пробег.

Отдельно в живых показаниях: «время в движении» считает только секунды со скоростью выше 2 км/ч
(`WheelData.java:40,170`), и средняя скорость поездки делится именно на него, а `avgSpeed` в базе
считается по всем отсчётам — две разные средние, и это у них осознанно.

**Что из этого следует для нас.** Записи на стоянке у нас уже нет только на словах: `AutoStartRide`
включён по умолчанию, значит подключение к стоящему у зарядки колесу и так начинает запись. Менять
схему и рвать CSV не нужно — нужно, чтобы **читатели** умели вычитать простои и не верить нулевому
одометру. Цена по месту: полмегабайта на час, ночь зарядки — четыре мегабайта.
