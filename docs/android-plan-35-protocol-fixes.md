# План 35. Фиксы протоколов — исполнение Части I мастер-плана оригиналов

> **Статус: составлен 15.08.2026, работы не начаты** — очерёдность этапов утверждает владелец
> ([мастер-план](originals-master-plan.md), «Что нужно решить владельцу» п. 1). Здесь — как
> исполнять: разбиение на задания, гейты, замки. Суть, тяжесть и источники каждого пункта — в
> мастер-плане, повторно не пересказываются.

## 0. Откуда план и что он покрывает

[Мастер-план разбора чужих приложений](originals-master-plan.md) собрал находки по пяти протоколам
и четырём родным приложениям. Его Часть I — «то, что сейчас работает неверно» — восемь пунктов
починки. Этот план превращает предложенную там очерёдность (этапы 0–10) в задания исполнителям.
Части II–IV (доработки протокола, новые возможности, архитектура) сюда не входят — им свои планы
([план 34](android-plan-34-wheel-settings.md) уже есть, остальное по слову владельца).

## 1. Правило порта — решить до первой правки

Порт протоколов 1:1 и правке не подлежит; изменения поведения — надстройкой (прецедент
`InMotionDecoderV2_1`), отклонения — в [port-deviations.md](port-deviations.md). Но пункты Части I
чинят места, где **наш порт совпадает с WheelLog, а WheelLog расходится с производителем**
(температура P6 — 26 строк формулы в шести раскладках). Надстройка над каждой строкой формулы —
дороже смысла.

**Предлагаемый порядок** (слово владельца): для таких мест — правка в порте **с записью в
port-deviations.md** («отклонение от WheelLog к производителю, причина, источник») и байтовым
тестом на обе стороны расхождения. Правило «порт сверяем с WheelLog» превращается в «порт сверяем
с WheelLog, кроме записанных отклонений» — как уже сделано с Trim хвоста алерта.

## 2. Этапы и задания

Нумерация — из мастер-плана. Каждое задание — отдельному мастеру со своим именем по задаче.

| Этап | Задание (имя мастера) | Гейт приёмки | Блокеры |
|---|---|---|---|
| 0 | **Выезд за данными** — владелец: журнал инцидента V14, дамп диагностики/BMS P6, счёт исходящих за поездку | данные лежат в replay/ | колесо, владелец |
| 1 | «Температура P6» — формула знакового байта во всех 26 местах + байтовые тесты горячей зоны (128↔127 граница), запись в port-deviations | тест обеих сторон границы; фикстуры P6 | решение §1 |
| 2 | «Потолок запросов KingSong» — пол 40 мс и потолок 50 попыток по образцу нашего Begode | замок: N уведомлений без ответа → ≤50 исходящих | — |
| 3 | «Тип записи InMotion» — подтверждаемая запись по образцу DarknessBot (~10 строк, один файл) | байтовый тест типа записи; **выпуск порознь с этапом 4** | — |
| 4 | «Опрос InMotion на таймер» — времязависимый опрос + счётчик раз за цикл, в надстройке V2_1; период настройкой (250–1000 мс) | **обязательный замок мастер-плана**: два прогона по виртуальному времени — пустой и с кадром после каждого запроса — число исходящих совпадает | — |
| 5 | Замер на колесе после 3 и 4 — **порознь** | журнал: темп исходящих, отвалы | владелец, колесо |
| 6 | «Приоритет соединения» — снять RequestConnectionPriority(High) | только после итогов этапа 5 | этап 5 |
| 7 | «Аварии P6» — разбор подкоманды диагностики (45 флагов) | дамп этапа 0; фикстуры из дампа | этап 0 |
| 8 | «Байтовые тесты Veteran» — тесты на уже отправляемые команды | чистое снятие риска, замки без правок поведения | — |
| 9 | «Begode: биты, кадры 5/6, ШИМ» — по слову владельца о битах (вопрос 3 мастер-плана) | фикстуры mten3-*; биты — решение владельца | вопрос 3 |
| 10 | «KingSong: код ошибки и кадры настроек» — словарь [kingsong-trouble-codes.md](kingsong-trouble-codes.md), раздельные обработчики | фикстуры; кадры настроек беречь под этап импорта | — |

## 3. Дисциплина исполнения

- **По одному этапу за заход**, этапы 3/4/6 — тремя отдельными выпусками с замером между: три
  лекарства разом не скажут, какое подействовало (мастер-план, «почему порознь»).
- Мастерам — источники из «Что нужно знать исполнителю»: рубрикатор `loeuc/AGENTS.md`, артефакты,
  наши записи; `loeuc` только чтение; **числа со ссылками файл:строка**.
- Грабли-семёрка мастер-плана — в каждое задание одной строкой: имя не доказательство, молчание
  не опровержение, чужое решение не истина.
- Гипотезы («частота опроса — причина отвала») начинаются с «проверить», не с «починить».

## 4. Ждёт владельца перед стартом

1. Утвердить очерёдность (или переставить) — п. 1 мастер-плана.
2. Решение §1 этого плана: правка порта с записью отклонения — или надстройка везде.
3. Этап 0: когда выезд и что снимать (без него этапы 1-финал, 7 и вся доказательность V14 висят).
4. Биты Begode: принять чужую подпись или ждать неисправного колеса (вопрос 3 мастер-плана).
