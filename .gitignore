bin/
obj/
.vs/
logs/
TestResults/
bash.exe.stackdump

# Профиль публикации (*.pubxml) в репозитории нужен — им публикуют из студии, и он обязан совпадать
# с тем, что делает dotnet publish. А *.pubxml.user — история запусков этой машины, не настройка.
*.pubxml.user

# Настройки подписи: пути к хранилищу ключей этой машины. Образец рядом — signing.props.example.
# Само хранилище и пароль к нему лежат вне репозитория и сюда не попадают в принципе.
signing.props
